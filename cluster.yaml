apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
   name: basic-cluster
   region: ap-southeast-1

nodeGroups:
   - name: ng-1
     instanceType: t3.medium
     desiredCapacity: 1
     minSize: 1
     maxSize: 2
     ssh:
        allow: false